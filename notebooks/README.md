# Notebooks

Use notebooks for transparent exploration and figure prototyping. Move stable,
reusable logic into `src/protein_ligand_md/` and test it under `tests/`.

The final notebook will be added only after validated simulation outputs exist.
It should restart and run from top to bottom without hidden state.

